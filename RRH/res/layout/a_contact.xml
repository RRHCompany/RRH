<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_bg_color"
    android:orientation="vertical" >

    <FrameLayout
        android:id="@+id/contact_top_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/app_bg_grey_color"
        android:paddingBottom="8dp"
        android:paddingLeft="15dp"
        android:paddingRight="15dp"
        android:paddingTop="8dp" >

        <EditText
            android:id="@+id/contact_listview_user_nickName_item"
            style="@style/edittext_style_small_radius_border"
            android:layout_height="40dp"
            android:layout_gravity="center_vertical|center_horizontal"
            android:focusable="false"
            android:gravity="center_vertical|center_horizontal"
            android:hint="@string/home_contact_search" />

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|center_horizontal"
            android:layout_marginRight="80dp"
            android:src="@drawable/contacts_seach_icon" />
    </FrameLayout>

    <ListView
        android:id="@+id/contact_friend_lv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/contact_top_layout"
        android:divider="@color/transparent"
        android:fadingEdge="none"
        android:fastScrollEnabled="false"
        android:scrollbars="none" />

    <com.cbkj.rrh.widget.SwitchGroup
        android:id="@+id/contact_friend_pinned"
        android:layout_width="25dp"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/contact_top_layout"
        app:orientation1="vertical"
        app:pinned_item1="@layout/view_switch_group_item"
        app:pinned_item_array1="@array/search_letters" />

</RelativeLayout>